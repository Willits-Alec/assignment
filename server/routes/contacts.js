const mongoose = require("mongoose");
const Contact = require('../models/contacts');
// const {getContacts} = require('../');

const express = require('express');
const router = express.Router();
module.exports = router; 

// const {getContacts}

// router.get('/', getContacts);
